# Documentation
This directory contains the Doxygen configuration files for building the HTML documentation of hummingbird. To build, ensure [doxygen](http://www.doxygen.org/) is installed, then run `make`. This generates a `documentation.html` file, which can be viewed in a web browser. To clean the documentation, run `make clean`.
